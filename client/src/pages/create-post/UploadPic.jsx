// things needed: pic[must], pic title, description and tags[must, 1 to 5 tags max]
// main album description + individual pic description if need
album making page[things needed: pic[must, 2 or more, upload new pic directly or use from existing], title, description and tags[must, 1 to 5 tags max]
]
